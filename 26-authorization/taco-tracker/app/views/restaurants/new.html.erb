<h1>Create a new restaurant</h1>

<%= form_for @restaurant do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :address %>
    <%= f.text_field :address %>

    <%= f.collection_check_boxes(:taco_ids, Taco.all, :id, :display_name) %>
    <%= f.submit %>

<% end %>